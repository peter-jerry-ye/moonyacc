{
  "pre-build": [
    {
      "command": "node $mod_dir/boot/moonlex.cjs $input | moonfmt > $output",
      "input": "lexer.mbtx",
      "output": "lexer.mbt"
    },
    {
      "command": "iwasm --module-path=$mod_dir/boot --dir=$mod_dir $mod_dir/boot/moonyacc.wasm --input-mode pull $input -o $output",
      "input": "parser.mbty",
      "output": "parser.mbt"
    }
  ],
  "import": [
    "moonbitlang/yacc/lib/ast",
    "moonbitlang/ulex-runtime/lexbuf"
  ]
}
