{
  "pre-build": [
    {
      "command": "iwasm --module-path=$mod_dir/boot --dir=$mod_dir $mod_dir/boot/moonyacc.wasm $input -o $output",
      "input": "inline.mbty",
      "output": "inline.mbt"
    }
  ],
  "warn-list": "-6"
}
