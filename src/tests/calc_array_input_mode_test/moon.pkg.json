{
  "pre-build": [
    {
      "command": "iwasm -v=0 --module-path=$mod_dir/boot --dir=$mod_dir $mod_dir/boot/moonyacc.wasm $input --input-mode array | moonfmt > $output",
      "input": "calc.mbty",
      "output": "calc.mbt"
    }
  ]
}
