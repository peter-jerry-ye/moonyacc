{
  "pre-build": [
    {
      "command": "iwasm -v=0 --module-path=$mod_dir/boot --dir=$mod_dir $mod_dir/boot/moonyacc.wasm --mode json-cst --input-mode pull --force-token-no-payload $input | moonfmt > $output",
      "input": "calc_jsoncst.mbty",
      "output": "calc_jsoncst.mbt"
    }
  ]
}
