{
  "pre-build": [
    {
      "command": "iwasm --module-path=$mod_dir/boot --dir=$mod_dir $mod_dir/boot/moonyacc.wasm $input --input-mode pull -o $output",
      "input": "calc.mbty",
      "output": "calc.mbt"
    }
  ]
}
